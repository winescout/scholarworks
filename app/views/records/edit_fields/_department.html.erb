<% campus = Hyrax::AdminSetService.new(controller).search_results(:deposit).first.title_or_label %>

<%= f.input :department, as: :select,
  collection: Hyrax::DepartmentsService.select_options(campus),
  input_html: { class: 'form-control', multiple: true }
%>
